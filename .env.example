NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only: required for moderation and listing analytics. Never use a NEXT_PUBLIC prefix.
SUPABASE_SERVICE_ROLE_KEY=
SEED_REALTOR_ID=

# Optional locally; set to your canonical HTTPS origin in production.
NEXT_PUBLIC_SITE_URL=

# Server-only random secret for pseudonymous listing statistics.
ANALYTICS_HASH_SECRET=
